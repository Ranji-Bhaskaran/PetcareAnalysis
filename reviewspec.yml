version: 0.2
env:
  secrets-manager:
    LOGIN: sonar/prod/gushy:sonartoken
    HOST: sonar/prod/gushy:HOST
    Organization: sonar/prod/gushy:Organization
    Project: sonar/prod/gushy:Project
phases:
  install:
    runtime-versions:
      java: corretto11
  pre_build:
    commands:
      - yum update -y
      - yum install -y epel-release
      - yum install -y jq
      - wget https://dlcdn.apache.org/maven/maven-3/3.9.5/binaries/apache-maven-3.9.5-bin.tar.gz
      - tar xzf apache-maven-3.9.5-bin.tar.gz
      - ln -s apache-maven-3.9.5 maven
      - wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-4.8.0.2856-linux.zip
      - unzip ./sonar-scanner-cli-4.8.0.2856-linux.zip
      - export PATH=$PATH:/sonar-scanner-4.8.0.2856-linux/bin/
  build:
    commands:
      - mvn test     
      - mvn sonar:sonar -Dsonar.login=$LOGIN -Dsonar.host.url=$HOST -Dsonar.projectKey=$Project -Dsonar.organization=$Organization
      - sleep 5
      - curl https://sonarcloud.io/api/qualitygates/project_status?projectKey=$Project > result.json
      - cat result.json
      - |
        if [ "$(jq -r '.projectStatus.status' result
